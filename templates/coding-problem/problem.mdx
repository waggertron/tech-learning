---
title: "<NNN>. Problem Title (Difficulty)"
description: "One sentence stating the key technique or insight."
parent: <category-slug>
tags: [leetcode, <category-slug>, <difficulty>]
status: draft
created: YYYY-MM-DD
updated: YYYY-MM-DD
---

import PythonRepl from '../../../../../../components/PythonRepl.astro';
import practiceCode from './<NNN>-<slug>.py?raw';

> **How to use this template**
> 1. Copy this file to `src/content/docs/topics/cs/coding-problems/<category>/<NNN>-<slug>.mdx`.
> 2. Add sibling solution files such as `<NNN>-<slug>.py`, `<NNN>-<slug>.ts`, and `<NNN>-<slug>.go` when the entry has runnable implementations.
> 3. Replace placeholder frontmatter, imports, links, and code.
> 4. Delete this blockquote.
> 5. Keep `## Related concepts` and link at least two concept pages.

## Problem

State the problem in your own words. Include the input, output, examples, and constraints.

**Examples**
- `input` -> `output`: why this output is correct.

**Constraints**
- Constraint one.
- Constraint two.

LeetCode <NNN> - [Link](https://leetcode.com/problems/<slug>/) - *Difficulty*

## Try it yourself

<PythonRepl code={practiceCode} id="<slug>" />

## Approach 1: Baseline

Explain the simplest correct idea. If this is not accepted at scale, say why.

```python
def solve():
    pass
```

| Line | Cost | Why |
| --- | --- | --- |
| Main work | $O(n)$ | Replace with the real dominant operation |

**Complexity**
- **Time:** $O(n)$
- **Space:** $O(1)$

### Further explanation

Add optional deeper reasoning here when it helps: invariants, recurrence, proof sketch, trace, edge cases, or why a tempting alternative fails.

## Approach 2: Optimal

Explain the standard tested solution.

```python
def solve():
    pass
```

**Complexity**
- **Time:** $O(n)$
- **Space:** $O(1)$

## How to recognize this pattern

- **Signal:** The phrase, constraint, or structure that points to the technique.
- **Counterexample:** The input that breaks the tempting approach.
- **Mental model:** The one sentence version of the right idea.

## Key takeaways

- Takeaway one.
- Takeaway two.
- Takeaway three.

## Related topics

- [Sibling problem](./sibling-problem/)
- [Category index](./)

## Related concepts

- [Two Pointers](../../coding-concepts/two-pointers/), why this concept matters for this problem.
- [Greedy Algorithms](../../coding-concepts/greedy-algorithms/), the invariant or proof shape behind the solution.
