---
title: "28. Find the Index of the First Occurrence in a String (Easy)"
description: "Given strings haystack and needle, return the index of the first occurrence of needle in haystack, or -1 if not found."
parent: sliding-window
tags: [leetcode, sliding-window, strings, easy]
status: draft
created: 2026-05-07
updated: 2026-05-07
---

import { Tabs, TabItem } from '@astrojs/starlight/components';
import PythonRepl from '../../../../../../components/PythonRepl.astro';
import TypeScriptRepl from '../../../../../../components/TypeScriptRepl.astro';
import GoRepl from '../../../../../../components/GoRepl.astro';
import practiceCode from './028-find-the-index-of-the-first-occurrence.py?raw';
import practiceCodeTs from './028-find-the-index-of-the-first-occurrence.ts?raw';
import practiceCodeGo from './028-find-the-index-of-the-first-occurrence.go?raw';
import approach2PyCode from './028-find-the-index-of-the-first-occurrence-approach2.py?raw';
import approach2TsCode from './028-find-the-index-of-the-first-occurrence-approach2.ts?raw';
import approach2GoCode from './028-find-the-index-of-the-first-occurrence-approach2.go?raw';

## Problem

Given two strings `haystack` and `needle`, return the index of the first occurrence of `needle` in `haystack`, or `-1` if `needle` is not part of `haystack`.

**Example**
- `haystack = "sadbutsad"`, `needle = "sad"` → `0`
- `haystack = "leetcode"`, `needle = "leeto"` → `-1`
- `haystack = "hello"`, `needle = "ll"` → `2`

LeetCode 28 · [Link](https://leetcode.com/problems/find-the-index-of-the-first-occurrence-in-a-string/) · *Easy*

## Try it yourself

<Tabs>
  <TabItem label="Python">
    <PythonRepl code={practiceCode} id="find-the-index-of-the-first-occurrence-py" />
  </TabItem>
  <TabItem label="TypeScript">
    <TypeScriptRepl code={practiceCodeTs} id="find-the-index-of-the-first-occurrence-ts" />
  </TabItem>
  <TabItem label="Go">
    <GoRepl code={practiceCodeGo} id="find-the-index-of-the-first-occurrence-go" />
  </TabItem>
</Tabs>

## Approach 1: Brute force sliding window

Slide a window of length `len(needle)` one character at a time over `haystack`. At each position, compare the window against `needle`. Return the first position where they match.

<Tabs>
<TabItem label="Python">
```python
def str_str(haystack: str, needle: str) -> int:
    n, m = len(haystack), len(needle)    # L1: O(1)
    for i in range(n - m + 1):          # L2: loop, n-m+1 iterations
        if haystack[i:i + m] == needle:  # L3: slice + compare, O(m) each
            return i                     # L4: O(1) return
    return -1                            # L5: O(1)
```
</TabItem>
<TabItem label="TypeScript">
```typescript
function strStr(haystack: string, needle: string): number {
    const n = haystack.length, m = needle.length;   // L1: O(1)
    for (let i = 0; i <= n - m; i++) {              // L2: loop, n-m+1 iterations
        if (haystack.slice(i, i + m) === needle) {  // L3: slice + compare, O(m) each
            return i;                               // L4: O(1) return
        }
    }
    return -1;                                      // L5: O(1)
}
```
</TabItem>
<TabItem label="Go">
```go
func strStr(haystack string, needle string) int {
	n, m := len(haystack), len(needle)      // L1: O(1)
	for i := 0; i <= n-m; i++ {            // L2: loop, n-m+1 iterations
		if haystack[i:i+m] == needle {      // L3: slice + compare, O(m) each
			return i                        // L4: O(1) return
		}
	}
	return -1                               // L5: O(1)
}
```
</TabItem>
</Tabs>

**Where the time goes, line by line**

*Variables: n = len(haystack), m = len(needle).*

| Line | Per-call cost | Times executed | Contribution |
| --- | --- | --- | --- |
| L1 (lengths) | $O(1)$ | 1 | $O(1)$ |
| L2 (loop) | $O(1)$ | n - m + 1 | $O(n)$ |
| **L3 (slice + compare)** | **$O(m)$** | **up to n - m + 1** | **$O(n * m)$** |
| L4 (return) | $O(1)$ | 1 | $O(1)$ |
| L5 (return -1) | $O(1)$ | 1 | $O(1)$ |

The slice `haystack[i:i+m]` allocates a new string of length `m` and the `==` comparison scans up to `m` characters. Done at each of the ~n positions, that is $O(n * m)$ total.

**Complexity**
- **Time:** $O(n * m)$, driven by L3 (window comparison at every position).
- **Space:** $O(m)$ for the slice allocated at each step.

## Approach 2: Built-in (interview-acceptable)

Python's `str.find()` and JavaScript's `String.prototype.indexOf()` use optimized algorithms under the hood. In an interview, returning the built-in result is explicitly acceptable for this problem.

<Tabs>
<TabItem label="Python">
```python
def str_str(haystack: str, needle: str) -> int:
    return haystack.find(needle)
```
</TabItem>
<TabItem label="TypeScript">
```typescript
function strStr(haystack: string, needle: string): number {
    return haystack.indexOf(needle);
}
```
</TabItem>
<TabItem label="Go">
```go
func strStr(haystack string, needle string) int {
	return strings.Index(haystack, needle)
}
```
</TabItem>
</Tabs>

This is $O(n)$ in the average case and $O(n * m)$ in the worst case, but the constant factor is far lower than a hand-rolled loop because the inner comparison runs in native code.

**Try this approach:**

<Tabs>
  <TabItem label="Python">
    <PythonRepl code={approach2PyCode} id="find-first-occurrence-approach2-py" />
  </TabItem>
  <TabItem label="TypeScript">
    <TypeScriptRepl code={approach2TsCode} id="find-first-occurrence-approach2-ts" />
  </TabItem>
  <TabItem label="Go">
    <GoRepl code={approach2GoCode} id="find-first-occurrence-approach2-go" />
  </TabItem>
</Tabs>

## Note on KMP

The Knuth-Morris-Pratt algorithm solves this in $O(n + m)$ time and $O(m)$ space by precomputing a failure function over `needle` that allows the search to skip redundant comparisons. See [KMP](../../../named-algorithms/kmp/) for the full walkthrough.

## Summary

| Approach | Time | Space |
| --- | --- | --- |
| Brute sliding window | $O(n * m)$ | $O(m)$ |
| Built-in | $O(n)$ avg | $O(1)$ |
| KMP | $O(n + m)$ | $O(m)$ |

The brute approach is the clearest to reason about and fine for small inputs. Use the built-in in interviews unless the interviewer specifically asks for the algorithm. KMP is the canonical $O(n)$ solution.

## Test cases

<Tabs>
<TabItem label="Python">
```python
# Quick smoke tests, paste into a REPL or save as test_028.py and run.
# Uses the brute sliding window implementation.

def str_str(haystack: str, needle: str) -> int:
    n, m = len(haystack), len(needle)
    for i in range(n - m + 1):
        if haystack[i:i + m] == needle:
            return i
    return -1

def _run_tests():
    assert str_str("sadbutsad", "sad") == 0
    assert str_str("leetcode", "leeto") == -1
    assert str_str("hello", "ll") == 2
    assert str_str("a", "a") == 0
    assert str_str("mississippi", "issip") == 4
    assert str_str("abc", "") == 0       # empty needle
    assert str_str("", "a") == -1        # empty haystack
    print("all tests pass")

if __name__ == "__main__":
    _run_tests()
```
</TabItem>
<TabItem label="TypeScript">
```typescript
function strStr(haystack: string, needle: string): number {
    const n = haystack.length, m = needle.length;
    for (let i = 0; i <= n - m; i++) {
        if (haystack.slice(i, i + m) === needle) return i;
    }
    return -1;
}

console.assert(strStr("sadbutsad", "sad") === 0);
console.assert(strStr("leetcode", "leeto") === -1);
console.assert(strStr("hello", "ll") === 2);
console.assert(strStr("a", "a") === 0);
console.assert(strStr("mississippi", "issip") === 4);
console.assert(strStr("abc", "") === 0);
console.assert(strStr("", "a") === -1);
console.log("all tests pass");
```
</TabItem>
<TabItem label="Go">
```go
func strStr(haystack string, needle string) int {
	n, m := len(haystack), len(needle)
	for i := 0; i <= n-m; i++ {
		if haystack[i:i+m] == needle {
			return i
		}
	}
	return -1
}
```
</TabItem>
</Tabs>


## Related topics

- [KMP algorithm](../../../named-algorithms/kmp/), the $O(n + m)$ solution to this exact problem
- [Permutation in String](../567-permutation-in-string/), fixed-size sliding window on strings
- [Longest Substring Without Repeating Characters](../003-longest-substring-without-repeating-characters/), variable sliding window

## Related concepts

- [Array Scans](../../../coding-concepts/array-scans/), the linear pass habit of carrying just enough state while reading each item once.
- [Two Pointers](../../../coding-concepts/two-pointers/), the two index invariant that shrinks or coordinates positions without nested loops.
